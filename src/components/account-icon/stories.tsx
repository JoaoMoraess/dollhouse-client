import { Story, Meta } from '@storybook/react/types-6-0'

import { AccountIcon } from '.'

export default {
  title: 'AccountIcon',
  component: AccountIcon
} as Meta

export const Default: Story = () => <AccountIcon />
